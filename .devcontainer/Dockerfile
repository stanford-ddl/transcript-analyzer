FROM mcr.microsoft.com/devcontainers/python:3.11

# Install any additional system-level dependencies here, if needed
RUN apt-get update && apt-get install -y --no-install-recommends \
    && rm -rf /var/lib/apt/lists/*